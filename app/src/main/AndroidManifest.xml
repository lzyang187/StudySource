<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lzy.studysource">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity android:name=".widget.WidgetActivity"></activity>-->
        <activity
            android:name=".lockscreen.ScreenActivity"
            android:launchMode="singleInstance"
            android:screenOrientation="portrait"
            android:theme="@style/locktheme" />

        <service
            android:name=".lockscreen.ScreenService"
            android:enabled="true"
            android:exported="true" /> <!-- <activity android:name=".radiogroup.RadioGroupActivity"></activity> -->
        <!-- <activity android:name=".headerfooter.HeaderAndFooterActivity"></activity> -->
        <!-- <activity android:name=".jetpack.workmanager.WorkManagerActivity"></activity> -->
        <!-- <activity android:name=".jetpack.camerax.CameraXActivity"></activity> -->
        <!-- <activity android:name=".viewpager2.ViewPager2Activity"></activity> -->
        <activity android:name=".ui.BaseActivity" />
        <!-- <activity android:name=".toolbar.ToolbarActivity"></activity> -->
        <!-- <activity android:name=".lrucache.LruCacheActivity"></activity> -->
        <!-- <activity android:name=".viewpager2.ViewPager2Activity"></activity> -->
        <!-- <activity android:name=".materialdesign.CardViewActivity"></activity> -->
        <activity android:name=".widget.WidgetActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".materialdesign.CoordinatorViewPagerActivity"></activity> -->
        <!-- <activity android:name=".materialdesign.CoordinatorLayoutActivity"></activity> -->
        <!-- <activity android:name=".snaphelper.SnapHelperActivity"></activity> -->
        <!-- <activity android:name=".jetpack.JetPackActivity"></activity> -->
        <!-- <activity android:name=".player.video.surfaceview.SurfaceViewActivity"></activity> -->
        <!-- <activity android:name=".muxerextractor.MuxerExtractorActivity"></activity> -->
        <!-- <activity android:name=".mediarecorder.MediaRecorderActivity"></activity> -->
        <!-- <activity android:name=".colormatrix.ColorMatrixActivity"></activity> -->
        <!-- <activity android:name=".constraint.ConstraintActivity" /> -->
        <!-- <activity android:name=".java.RecyclerViewActivity"></activity> -->
        <!-- <activity android:name=".WebViewActivity"></activity> -->
        <!-- <activity android:name=".sms.SendSmsActivity"></activity> -->
        <service
            android:name=".job.MyJobService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>

</manifest>